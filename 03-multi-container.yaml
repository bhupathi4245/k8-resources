apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  labels:
    app: roboshop
    purpose: multi-containder-demo
spec:
  containers:
    - name: nginx
      image: nginx
    - name: almalinux
      image: almalinux
      command: ["sleep", "1000"]